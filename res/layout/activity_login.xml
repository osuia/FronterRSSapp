<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/grad"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="15dp"
    android:paddingRight="15dp"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LoginActivity" >

    <RelativeLayout
        android:id="@+id/whiteBackground"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/imageView1"
        android:layout_centerHorizontal="true"
        android:background="@android:color/white"
        android:paddingTop="5dp" >

        <EditText
            android:id="@+id/user_UserName"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:ems="10"
            android:hint="brukernavn"
            android:inputType="text" />

        <EditText
            android:id="@+id/user_Password"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/user_UserName"
            android:layout_centerHorizontal="true"
            android:ems="10"
            android:hint="passord"
            android:inputType="textPassword" />

        <EditText
            android:id="@+id/user_Installation"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/user_Password"
            android:ems="10"
            android:hint="uia"
            android:inputType="text" />

        <Button
            android:id="@+id/button1"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/user_Installation"
            android:background="@drawable/login_btn"
            android:clickable="true"
            android:minHeight="60dp"
            android:onClick="login"
            android:text="logg inn"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />
    </RelativeLayout>

    <ImageView
        android:id="@+id/imageView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/whiteBackground"
        android:layout_alignParentTop="true"
        android:paddingBottom="25dp"
        android:paddingTop="15dp"
        android:src="@drawable/fronter" />

</RelativeLayout>